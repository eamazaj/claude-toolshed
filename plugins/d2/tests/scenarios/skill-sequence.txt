Create a sequence diagram showing a JWT authentication flow: the client sends credentials to the API gateway, which forwards to the auth service, which validates against the database and returns a JWT token back to the client. Include the happy path and the error case (invalid credentials).

Save the diagram to ./diagrams with a proper filename. Use D2 format (.d2 extension).
